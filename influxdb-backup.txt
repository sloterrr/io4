docker ps

CONTAINER ID:
f3b50c005d21 

backup sintase:
3
docker exec -it <container_id> influxd backup -portable -db <dbName> <folder-to-save-backup>

just replace the tag with real value, (if you have more than one database to backup you can skip the tag -db) influxdb will backup all the databases. In my example we try to backup a database called “mydbname” and we save it into “/home/user/backup-dir”.

docker exec -it f6dd2b719f88 influxd backup -portable -database mydbname /home/user/backup-dir



Sintax
docker cp <container_id>:<folder-local-backup> <folder-remote-container-backup>

docker cp f6dd2b719f88: /backup-dir /home/user/backup-dir
Now we have the full database’s backup ready to restore just in case.

So what we are waiting for?? Lest try to restore it!!!



INLFUX CLI
docker exec -it influxdb /bin/bash



docker exec -it addon_a0d7b954_influxdb influx -username myuser -password mypassword